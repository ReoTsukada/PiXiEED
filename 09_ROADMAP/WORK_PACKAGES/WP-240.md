# WP-240 — Admin, Analytics, Ads, Moderation, and Revenue Projection

Status: `complete pending external audit`

Source status: `RECONSTRUCTED_FROM_APPROVED_SOURCES`

## Scope authority and provenance

No dedicated WP-240 package existed. This scope is reconstructed from the queue/roadmap row and
`FEATURE_FLAG_OBSERVABILITY_ROLLBACK_CORE.md`, `ACCOUNT_PERMISSION_CORE.md`,
`EVENT_ACTIVITY_CORE.md`, `SEARCH_INDEX_CORE.md`, `NOTIFICATION_CORE.md`,
`EXISTING_PLATFORM_PRESERVATION.md`, the existing owner/admin RPC entry points, and the existing
advertising policy documents. Existing Admin, Market, Ads, Database, and production paths are
evidence only; no production path is imported.

## Canonical objective

Build an unloaded, deterministic read/shadow boundary for permission-gated Admin projections,
privacy-safe Analytics, public-only ad placement, moderation references, and shadow-only revenue
comparison. Admin capability comes from server-resolved permission; client role/override is never
authority. Analytics contains only bounded metadata. Active Studio, Checkout, Entitlement, and
Commission surfaces never receive ads. Moderation references do not copy content or mutate targets.
Revenue comparison never mutates Ledger or Payout.

## Implementation

- `pixiedraw2/src/wp240-admin-analytics-ads-core.ts`
- `pixiedraw2/src/wp240-bundle-entry.ts`
- `pixiedraw2/tests/wp240-admin-analytics-ads.test.ts`
- `pixiedraw2/benchmarks/run-wp240-admin-analytics-benchmark.ts`
- `scripts/test-pixiedraw2-wp240.mjs`
- `scripts/measure-pixiedraw2-wp240-bundle.mjs`

All five domain flags default OFF; unknown flags and kill switch fail closed. Rollback is local
shadow-only. The module has no DOM, route, storage, provider, database, or financial side effect.

## Acceptance and release truth

The synthetic checks cover Admin server permission, Analytics privacy rejection, public/private ad
surfaces, moderation reference safety, revenue shadow-only invariants, flags, rollback, static
lazy-boundary checks, and the inherited 14 Baseline Failure Identities with new identity count 0.
Real Admin RPC/RLS, Analytics pipeline, ad delivery, moderation console, financial reconciliation,
production performance, and deployment remain UNTESTED. No migration, route cutover, publish,
deploy, commit, or push was performed.

