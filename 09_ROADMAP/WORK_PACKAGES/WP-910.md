# WP-910 — G0 Canonical Completeness Qualification

status: PLANNED
phase: qualification
kind: qualification
depends_on: WP-900
implementation_model: Terra High
implementation_parallelism: max-5-disjoint-tracks
review_model: Terra High
independent_review: true
next_package: WP-920
auto_start_next: false
verification_level: production-equivalent-evidence-required

## Canonical role

Verify cross-system journeys and the canonical inputs required to execute them. The title remains G0 Canonical Completeness; this package also owns the first observed journey pass. This definition is subordinate to `00_START_HERE/WORK_PACKAGE_REGISTRY.json`; the Registry owns status, dependencies, routes, write scope, and handoff truth.

## Required journeys

- Authoring: Draw2 revision -> Asset Registry -> Game LIVE/PINNED preview -> Audio revision -> Package -> Build -> Runtime.
- Commerce: Product -> Checkout/provider event -> Purchase -> Entitlement -> License -> Royalty -> Ledger -> Notification.
- Community: public work -> Core card -> Search -> Social share -> Notification -> moderation/visibility.
- Direct Work: Request -> Quote -> Agreement -> Milestone -> Delivery -> Acceptance -> Rights -> Payment -> Ledger.

## Acceptance and evidence

- `WP910-CANONICAL-001`: For every journey, cite the Project/Asset/Revision/Package/Authorization/Money/Event contract and verify input/output identity, version, dependency lock, and failure diagnostic. A file or class existing is not evidence.
- `WP910-PROVENANCE-001`: Execute each journey on sanitized fixtures with a trace table of command/event IDs, revisions, hashes, authorization proof, and resulting state. Confirm no caller-provided authority, amount, recipient, or stale record is accepted.
- `WP910-MISSING-001`: Inject missing asset, stale revision, denied capability, duplicate event, out-of-order event, offline state, provider error, malformed package, and notification failure. Each path must fail closed, preserve canonical state, and expose a recoverable user-facing result.
- `WP910-CROSSFLOW-001`: Re-run the four journeys across Browser fake and each implemented isolated Host Adapter. Compare canonical outputs byte/hash deterministically; local panel/tool/zoom state must not enter canonical operations.
- `WP910-BOUNDARY-001`: Verify Draw2/Audio/Game/Runtime/Market/Social/Notification boundaries remain feature-flagged and do not alter current routes, PXD, PiXiSYNC, Market, production data, or fallback.

## Evidence contract

Each journey needs a step table, fixture/hash, environment, command/event trace, raw output, screenshots for user-visible states, negative-case result, reviewer, timestamp, and status. Full E2E, actual provider, actual device, and production evidence are separate claims. Missing evidence remains `UNTESTED`; predecessor baseline failure identity must match with new failures=0.

## Bounded write scope

- `docs/qualification/WP-910/**`
- `scripts/qualification/wp-910/**`
- `docs/contracts/WP-910-*.md`
- `docs/inventory/wp-910-*.json`
- `docs/decisions/ADR-*-WP-910-*.md`
- Coordinator-owned Queue, Registry, State, Worklog, and generated Context manifests are not package-agent write targets.

## Stop rules

- Preserve current Market, PiXiSYNC, URLs, data, production fallback, and dirty worktree.
- Do not migrate, deploy, publish, commit, push, or start the next package automatically.
- Stop on any cross-system identity mismatch, unhandled failure, authority ambiguity, new baseline failure, stale Context, or data-loss risk.

## Handoff

Complete only after journey traces, failure matrix, Checkpoint, and independent review are recorded. The next package is `WP-920`; `autoStartNext: false`. Wait for explicit instruction.
