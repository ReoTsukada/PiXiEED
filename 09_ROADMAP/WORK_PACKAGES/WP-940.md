# WP-940 — G3 Performance and Memory Qualification

status: PLANNED
phase: qualification
kind: qualification
depends_on: WP-930
implementation_model: Terra High
implementation_parallelism: max-5-disjoint-tracks
review_model: Terra High
independent_review: true
next_package: WP-950
auto_start_next: false
verification_level: production-equivalent-evidence-required

## Canonical role

Qualify legacy PXD, existing Projects, PiXiSYNC, URLs, Market, Commerce, and no-data-loss compatibility. The title remains G3 Performance and Memory Qualification; this package is the compatibility qualification step in the current canonical sequence. This definition is subordinate to `00_START_HERE/WORK_PACKAGE_REGISTRY.json`; the Registry owns status, dependencies, routes, write scope, and handoff truth.

## Acceptance and evidence

- `WP940-BUNDLE-001`: For approved sanitized Legacy PXD fixtures, read without in-place rewrite; compare pixels, palette, layers, frames, cels, durations, transparency, export bytes/hash, and original preservation. Record fixture provenance and conversion warnings.
- `WP940-LATENCY-001`: Open existing Project fixtures through current and new adapters, compare Project/Asset/Revision identity, checkpoint/journal recovery, visible output, and failure rollback. Any real-user fixture requires Owner-approved sanitization.
- `WP940-MEMORY-001`: Test old/new PiXiSYNC coexistence, operation ordering, duplicate/replay, reconnect, permissions, checkpoint, and rollback using isolated accounts/projects. Workspace panel, zoom, local tool, and scroll state must not sync.
- `WP940-DEVICE-001`: Verify existing URLs and Market product/file/license/entitlement paths remain resolvable, withdrawn/private visibility remains unchanged, no repurchase is requested, and no production rows/storage are touched. Record read-only hash/reconciliation output.
- `WP940-NODATALOSS-001`: Inject malformed/unsupported PXD, missing blob, stale revision, checksum mismatch, interrupted import/export, quota error, and adapter failure. Preserve the original and return a typed recovery diagnostic.

## Evidence contract

Each result contains fixture provenance/sanitization approval, source and output hash, client/adapter version, read-only protocol, visual/reconciliation output, negative result, reviewer, timestamp, and status. “Compatible by design” is not evidence. Real production/user data, real purchase, and real PiXiSYNC evidence remain `UNTESTED` when unavailable; baseline failure identity must match with new failures=0.

## Bounded write scope

- `docs/qualification/WP-940/**`
- `scripts/qualification/wp-940/**`
- `docs/contracts/WP-940-*.md`
- `docs/inventory/wp-940-*.json`
- `docs/decisions/ADR-*-WP-940-*.md`
- Coordinator-owned Queue, Registry, State, Worklog, and generated Context manifests are not package-agent write targets.

## Stop rules

- Preserve current Market, PiXiSYNC, URLs, data, production fallback, and dirty worktree.
- Do not migrate, deploy, publish, commit, push, rewrite legacy data, or start the next package automatically.
- Stop on hash/visual/identity mismatch, missing rollback, privacy boundary uncertainty, new baseline failure, stale Context, or data-loss risk.

## Handoff

Complete only after PXD/project/PiXiSYNC/commerce compatibility matrix, Checkpoint, and independent review are recorded. The next package is `WP-950`; `autoStartNext: false`. Wait for explicit instruction.
