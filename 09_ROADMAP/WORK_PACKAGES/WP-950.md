# WP-950 — G4 Legacy Compatibility Qualification

status: PLANNED
phase: qualification
kind: qualification
depends_on: WP-940
implementation_model: Terra High
implementation_parallelism: max-5-disjoint-tracks
review_model: Terra High
independent_review: true
next_package: WP-960
auto_start_next: false
verification_level: production-equivalent-evidence-required

## Canonical role

Qualify Auth, RLS, RPC, provider, visibility, path, webhook, replay, and redaction boundaries. The title remains G4 Legacy Compatibility Qualification; this package is the security qualification step in the current canonical sequence. This definition is subordinate to `00_START_HERE/WORK_PACKAGE_REGISTRY.json`; the Registry owns status, dependencies, routes, write scope, and handoff truth.

## Acceptance and evidence

- `WP950-PXD-001`: Test authenticated/anonymous/other-tenant/expired/revoked principals against Project, Asset, Package, Search, Notification, Social, Market, and Direct Work resources. Verify `AuthorizationProofV1` binding for principal, tenant, resource, action, capability, and expiry; caller-created fake/clone/Proxy/JSON proof is denied.
- `WP950-PROJECT-001`: Run RLS and RPC allowed/denied matrices for read/write/publish/download/recipient/ledger actions, including resource ID substitution, stale membership, provider substitution, and replay. Record SQL/RPC result and typed error without exposing secrets.
- `WP950-PIXISYNC-001`: Test webhook/provider identity validation, duplicate/out-of-order/replayed event, safe storage path, traversal/encoded separator, MIME/size, malformed JSON, SSRF-like locator, and private content redaction. No JWT, email, project contents, personal request, payment data, or secret enters telemetry.
- `WP950-MARKET-001`: Verify Product, Purchase, Entitlement, License, Royalty, Ledger, Commission, and Notification authority is server-resolved. Caller amount/currency/recipient/rate/entry type and Boolean trust cannot decide outcome.
- `WP950-NODATALOSS-001`: Confirm deny paths are fail-closed, idempotent, non-mutating, and recoverable; approved fixtures remain unchanged. New P0 security/privacy findings are zero for the proposed scope.

## Evidence contract

Each case records actor class, tenant/resource/action, request correlation ID, safe response/error class, RLS/RPC/provider result, fixture/hash, reviewer, timestamp, and status. Production provider/RLS access is not implied by isolated tests; unavailable provider, production, and real-user evidence remains `UNTESTED`. Baseline failure identity must match and new failures must be zero.

## Bounded write scope

- `docs/qualification/WP-950/**`
- `scripts/qualification/wp-950/**`
- `docs/contracts/WP-950-*.md`
- `docs/inventory/wp-950-*.json`
- `docs/decisions/ADR-*-WP-950-*.md`
- Coordinator-owned Queue, Registry, State, Worklog, and generated Context manifests are not package-agent write targets.

## Stop rules

- Preserve current Market, PiXiSYNC, URLs, data, production fallback, and dirty worktree.
- Do not migrate, deploy, publish, commit, push, mutate production RLS/RPC/provider state, or start the next package automatically.
- Stop on caller-controlled authority, cross-tenant read/write, secret/PII leakage, unsafe path/provider, replay acceptance, new baseline failure, or data-loss risk.

## Handoff

Complete only after independent attack matrix, server-boundary evidence, Checkpoint, and review are recorded. The next package is `WP-960`; `autoStartNext: false`. Wait for explicit instruction.
