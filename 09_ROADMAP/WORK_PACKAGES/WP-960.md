# WP-960 — G5 Security / Permission / RLS Qualification

status: PLANNED
phase: qualification
kind: qualification
depends_on: WP-950
implementation_model: Sol MAX
implementation_parallelism: max-5-disjoint-tracks
review_model: Sol High
independent_review: true
next_package: WP-970
auto_start_next: false
verification_level: production-equivalent-evidence-required

## Canonical role

Qualify production-equivalent staging, database/storage adapters, flags, observability, and rollback controls without touching Production. The title remains G5 Security / Permission / RLS Qualification; this package is the staging rehearsal step in the current canonical sequence. This definition is subordinate to `00_START_HERE/WORK_PACKAGE_REGISTRY.json`; the Registry owns status, dependencies, routes, write scope, and handoff truth.

## Acceptance and evidence

- `WP960-AUTH-001`: Build an isolated Production-equivalent environment with versioned source, database schema, Storage fixture, Auth/Provider fake or approved staging adapter, Feature Flag defaults, and artifact manifest. Record environment identity and no-Production-target proof.
- `WP960-RLS-001`: Re-run representative RLS/RPC/provider/Storage/route tests in staging, including deny, timeout, duplicate, stale, and rollback cases. Compare schema/function/policy/storage hashes to the declared artifact.
- `WP960-VISIBILITY-001`: Exercise Browser/PWA, Host Adapter, current fallback, new isolated route, Kill Switch, read/write flags, notifications, Market, PiXiSYNC, and direct-work paths. Current route remains the safe fallback and page overflow remains zero in supported shells.
- `WP960-WEBHOOK-001`: Rehearse provider webhook signature, idempotency, out-of-order, retry, quarantine, redaction, and alert paths with no real payment/payout/ad event. Record event IDs and reconciliation result.
- `WP960-REDACTION-001`: Verify Audit/Telemetry excludes JWT, email, secret, Project content, personal request content, payment, and license-sensitive data; verify monitoring has useful typed counters without payload leakage.
- `WP960-ROLLBACK-001`: Toggle the kill switch and read/write flags in staging, restore previous artifact/config, and prove canonical state is preserved. A documented rollback without a rehearsal remains `UNTESTED`.

## Evidence contract

Record environment IDs, source/artifact/schema/storage hashes, seed fixture manifest, flag state, command/protocol, raw logs with redaction scan, screenshots, rollback result, reviewer, timestamp, and status. Production-equivalent is not Production; unavailable cloud/provider/staging evidence remains `UNTESTED`. Baseline failure identity must match with new failures=0.

## Bounded write scope

- `docs/qualification/WP-960/**`
- `scripts/qualification/wp-960/**`
- `docs/contracts/WP-960-*.md`
- `docs/inventory/wp-960-*.json`
- `docs/decisions/ADR-*-WP-960-*.md`
- Coordinator-owned Queue, Registry, State, Worklog, and generated Context manifests are not package-agent write targets.

## Stop rules

- Preserve current Market, PiXiSYNC, URLs, data, production fallback, and dirty worktree.
- Do not apply production migration, deploy, publish, commit, push, alter production RLS/RPC/provider/config, or start the next package automatically.
- Stop on environment drift, missing redaction, failed kill switch, route fallback failure, new baseline failure, stale Context, or data-loss risk.

## Handoff

Complete only after staging manifest, boundary matrix, redaction scan, rollback rehearsal, Checkpoint, and independent review are recorded. The next package is `WP-970`; `autoStartNext: false`. Wait for explicit instruction.
