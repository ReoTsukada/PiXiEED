{
  "name": "@pixieed/reference-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Executable reference contracts for PiXiEED command, journal, checkpoint, and raster foundations.",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "benchmark:tiles": "node scripts/benchmark-tile-sizes.mjs"
  }
}
