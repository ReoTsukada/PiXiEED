import { createDraw160Preview, reloadDraw160Preview, type CanonicalPreviewResolver, type CanonicalPreviewRevision } from "../../src/draw2/draw-160/preview-boundary.ts";
import type { RuntimeCapabilityProfile } from "../../src/wp160-contracts.ts";
const record: CanonicalPreviewRevision = { source: "CANONICAL_REGISTRY", assetId: "sprite/player", revisionId: "rev-a", contentHash: "a".repeat(64), byteLength: 32, mimeType: "application/octet-stream", mode: "LIVE", licenseStatus: "VALID", permission: "GRANTED" };
const resolver: CanonicalPreviewResolver = { resolveLive: async () => record, resolvePinned: async () => undefined, resolveReview: async () => undefined, resolveForked: async () => undefined };
const capabilities: RuntimeCapabilityProfile = { pointer: true, touch: true, keyboard: true, mouse: true, gamepad: false, screenWidth: 1280, screenHeight: 720, devicePixelRatio: 1, audio: false, graphics: "CANVAS2D", webGpuBenefitMeasured: false, reducedMotion: false };
const controller = await createDraw160Preview({ previewId: "draw-160-benchmark", projectId: "project-1", projectRevisionId: "project-rev-1", packageId: "package-1", packageVersion: "1.0.0", runtime: { runtimeId: "pixie-runtime", runtimeVersion: "0.1.0", supportedManifestVersion: 1 }, supportedRuntimeVersion: "0.1.0", capabilities, resolver, request: { assetId: "sprite/player", mode: "LIVE" } });
const iterations = 1000; const started = performance.now(); let current = controller;
for (let i = 0; i < iterations; i += 1) { const result = await reloadDraw160Preview(current, { packageId: "package-1", packageVersion: "1.0.0", resolver, request: { assetId: "sprite/player", mode: "LIVE" } }); current = result.controller; }
console.log(JSON.stringify({ packageId: "DRAW-160", benchmark: "canonical-live-reload-resolution", iterations, elapsedMs: Number((performance.now() - started).toFixed(3)), status: "MEASURED_LOCAL_SYNTHETIC", notes: ["Not a browser/device/full compositor/production performance gate."] }));
