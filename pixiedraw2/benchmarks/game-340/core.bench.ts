import { asAssetId, asAssetRevisionId, asBehaviorId, asComponentId, asDependencyId, asEntityId, asOwnerId, asProjectId, asRevisionId, asSceneId, asSha256, compileNoCodeBehavior, createGameProject, type CallerContext, type GameProjectDraft } from "../../src/game/game-300/core.ts";
import { createIntegratedPackage, type AssetBinding, type CanonicalAssetRevision, type DependencyLock, type LicenseSnapshot } from "../../src/game/game-340/core.ts";
const owner = asOwnerId("owner-bench-340"); const projectId = asProjectId("project-bench-340"); const revisionId = asRevisionId("revision-bench-340"); const caller: CallerContext = { projectId, ownerId: owner, revisionId }; const hash = asSha256("a".repeat(64));
const behavior = compileNoCodeBehavior({ behaviorId: asBehaviorId("behavior-bench-340"), rules: [] }); const draft: GameProjectDraft = { schemaVersion: 1, projectId, ownerId: owner, name: "bench", revision: { revisionId, projectId, ownerId: owner, sequence: 1 }, scenes: [{ sceneId: asSceneId("scene-bench-340"), name: "Main", rootEntityIds: [asEntityId("entity-bench-340")], entities: [{ entityId: asEntityId("entity-bench-340"), name: "E", components: [{ type: "TRANSFORM", componentId: asComponentId("transform-bench-340"), x: 0, y: 0, rotation: 0, scaleX: 1, scaleY: 1 }, { type: "SPRITE", componentId: asComponentId("sprite-bench-340"), asset: { kind: "DRAW", assetId: asAssetId("draw-bench-340"), revisionId: asAssetRevisionId("draw-rev"), ownerId: owner, contentHash: hash, mode: "LIVE" }, visible: true }] }] }], prefabs: [], dependencies: [{ dependencyId: asDependencyId("dep-bench-340"), kind: "PACKAGE", ownerId: owner, ownerRevisionId: revisionId, targetId: "runtime", targetRevisionId: "1", dependsOn: [] }], behaviors: [behavior] };
const project = await createGameProject(draft, caller); const authority: CanonicalAssetRevision[] = [{ projectId: String(projectId), ownerId: String(owner), kind: "DRAW", assetId: "draw-bench-340", revisionId: "draw-rev", contentHash: hash, licenseId: "license-bench", permission: "PREVIEW", reviewStatus: "APPROVED" }]; const bindings: AssetBinding[] = [{ componentId: "sprite-bench-340", projectId: String(projectId), ownerId: String(owner), kind: "DRAW", assetId: "draw-bench-340", mode: "LIVE", licenseId: "license-bench", permission: "PREVIEW" }]; const deps: DependencyLock[] = [{ dependencyId: "dep-bench-340", targetId: "runtime", targetRevisionId: "1", contentHash: hash, licenseId: "license-bench", dependsOn: [] }]; const licenses: LicenseSnapshot[] = [{ licenseId: "license-bench", ownerId: String(owner), scope: "PACKAGE", revisionId: String(revisionId) }];
for (let i = 0; i < 10; i++) { const started = performance.now(); const result = await createIntegratedPackage(project, caller, bindings, authority, deps, licenses); console.log(JSON.stringify({ iteration: i + 1, ok: result.ok, ms: Number((performance.now() - started).toFixed(3)), assets: result.value?.assetLocks.length ?? 0 })); }
